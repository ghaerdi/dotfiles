@import "config"

* {
		primary:	 @red;
		secondary: @red;
}

configuration {
		modi: "p:rofi-power-menu --choices=shutdown/reboot";
    sidebar-mode:	false;
		display-p: "POWERMENU";
}

window {
    width:	300px;
    height:	180;
}
